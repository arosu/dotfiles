# Alex’s dotfiles

## Installation
Install the files to your home directory.
```bash
git clone --recursive git@github.com:arosu/dotfiles.git ~/dotfiles
cd ~/dotfiles
./update && ./install
```

Setup some sensible Mac defaults and install some common homebrew formulae.
```bash
./.macos
./brew.sh
```
